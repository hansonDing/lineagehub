/**
 * SQL 管理页(Sql)词条 —— 桩文件
 * 由后续 i18n-sql agent 填充;key 统一带 `sql.` 前缀(如 `sql.editor.placeholder`)。
 * 注意:zh/en 两个字典必须同步增减;共享的状态/层名/角色/按钮文案请复用 common.ts 中的 `common.*` key。
 */
export const zh: Record<string, string> = {}
export const en: Record<string, string> = {}
